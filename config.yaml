data:
  filepath: "data/bank_churn.csv"
  test_size: 0.2
  target_column: "Exited"
  drop_columns: ["RowNumber", "CustomerId", "Surname"] 

model:
  n_estimators: 100
  max_depth: 5
  random_state: 42
  threshold: 0.5

api:
  port: 8000
  host: "0.0.0.0"

monitoring:
  drift_threshold: 0.1